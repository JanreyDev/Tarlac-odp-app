import type React from "react"
import type { Metadata } from "next"
import { Inter, Geist_Mono } from "next/font/google"
import { Analytics } from "@vercel/analytics/next"
import "./globals.css"

const _inter = Inter({ subsets: ["latin"] })
const _geistMono = Geist_Mono({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Tarlac Open Data Portal | data.tarlac.gov.ph",
  description:
    "The official provincial platform for accessing and exploring public datasets from Tarlac Province. Discover education, health, agriculture, infrastructure, and governance data.",
  keywords: ["Tarlac", "Open Data", "Philippines", "Government", "Datasets", "Public Data", "Transparency"],
    generator: 'v0.app'
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className="font-sans antialiased">
        {children}
        <Analytics />
      </body>
    </html>
  )
}
